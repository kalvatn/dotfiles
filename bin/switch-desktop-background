#!/bin/bash

set -e
source "$HOME/.functions"
image_file="$(random_image)"
symlink "$image_file" "$HOME/.current-desktop-background"
switch-greeter-background "$image_file"
feh --no-fehbg --bg-fill "$image_file"
