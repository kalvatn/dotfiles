#!/bin/bash

set -e
source "$HOME/.functions"
BACKGROUND_IMAGE_LINK="$HOME/.current-desktop-background"

if [ -z "$1" ]; then
	image_file="$(random-image)"
else
	image_file="$1"
fi
symlink "$image_file" "$BACKGROUND_IMAGE_LINK"

feh --no-fehbg --bg-fill "$image_file"
